<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="personal.nfl.opencv.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.CameraPreviewActivity" />
        <activity android:name=".activity.MixedProcessingActivity" />
        <activity android:name=".activity.CameraControlActivity" />
        <activity android:name=".activity.Image2GreyActivity" />
        <activity android:name=".activity.ImageManipulationsActivity" />
        <activity android:name=".activity.FaceDetectActivity" />
        <activity android:name=".activity.ColorBlobDetectionActivity"></activity>
        <activity android:name=".activity.CameraCalibrationActivity">

        </activity>
        <activity android:name=".activity.Puzzle15Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>